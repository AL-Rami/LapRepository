Iimport React, { useState } from 'react';
import { 
  FlaskConical, 
  ClipboardList, 
  ShieldAlert, 
  Stethoscope, 
  Flame, 
  Droplets, 
  CheckCircle, 
  AlertTriangle,
  Menu,
  X,
  Thermometer,
  Wind,
  DoorOpen,
  Eye,
  Activity,
  Box,
  Ban
} from 'lucide-react';

const SchoolLabGuide = () => {
  const [isMenuOpen, setIsMenuOpen] = useState(false);

  const toggleMenu = () => setIsMenuOpen(!isMenuOpen);

  const scrollToSection = (id) => {
    const element = document.getElementById(id);
    if (element) {
      element.scrollIntoView({ behavior: 'smooth' });
      setIsMenuOpen(false);
    }
  };

  const navItems = [
    { id: 'duties', label: 'مهام المحضر', icon: <ClipboardList size={18} /> },
    { id: 'safety', label: 'السلامة والمختبر', icon: <ShieldAlert size={18} /> },
    { id: 'firstaid', label: 'الإسعافات الأولية', icon: <Stethoscope size={18} /> },
    { id: 'chemicals', label: 'المواد الكيميائية', icon: <FlaskConical size={18} /> },
    { id: 'solutions', label: 'تحضير المحاليل', icon: <Droplets size={18} /> },
    { id: 'fire', label: 'مكافحة الحريق', icon: <Flame size={18} /> },
  ];

  return (
    <div dir="rtl" className="font-sans text-slate-800 bg-slate-50 min-h-screen">
      {/* Navigation */}
      <nav className="bg-white shadow-md sticky top-0 z-50">
        <div className="max-w-7xl mx-auto px-4 sm:px-6 lg:px-8">
          <div className="flex justify-between h-16">
            <div className="flex items-center">
              <FlaskConical className="h-8 w-8 text-cyan-600 ml-2" />
              <span className="font-bold text-xl text-slate-800">دليل المختبرات</span>
            </div>
            
            {/* Desktop Menu */}
            <div className="hidden md:flex items-center space-x-reverse space-x-4">
              {navItems.map((item) => (
                <button
                  key={item.id}
                  onClick={() => scrollToSection(item.id)}
                  className="flex items-center gap-2 px-3 py-2 rounded-md text-sm font-medium text-slate-600 hover:text-cyan-600 hover:bg-cyan-50 transition-colors"
                >
                  {item.icon}
                  {item.label}
                </button>
              ))}
            </div>

            {/* Mobile Menu Button */}
            <div className="md:hidden flex items-center">
              <button
                onClick={toggleMenu}
                className="p-2 rounded-md text-slate-600 hover:text-cyan-600 focus:outline-none"
              >
                {isMenuOpen ? <X size={24} /> : <Menu size={24} />}
              </button>
            </div>
          </div>
        </div>

        {/* Mobile Menu Dropdown */}
        {isMenuOpen && (
          <div className="md:hidden bg-white border-t">
            <div className="px-2 pt-2 pb-3 space-y-1 sm:px-3">
              {navItems.map((item) => (
                <button
                  key={item.id}
                  onClick={() => scrollToSection(item.id)}
                  className="flex items-center gap-3 w-full text-right px-3 py-2 rounded-md text-base font-medium text-slate-600 hover:text-cyan-600 hover:bg-cyan-50"
                >
                  {item.icon}
                  {item.label}
                </button>
              ))}
            </div>
          </div>
        )}
      </nav>

      {/* Hero Section */}
      <div className="bg-gradient-to-br from-cyan-600 to-blue-800 text-white py-20 px-4 text-center relative overflow-hidden">
        <div className="absolute top-0 left-0 w-full h-full opacity-10">
          <FlaskConical size={400} className="absolute -left-20 -bottom-20 transform rotate-12" />
        </div>
        <div className="max-w-4xl mx-auto relative z-10">
          <h1 className="text-4xl md:text-6xl font-bold mb-6">دليل المختبرات المدرسية الشامل</h1>
          <p className="text-xl md:text-2xl text-cyan-100 mb-8 font-light">خلاصة شاملة للسلامة والكفاءة المهنية</p>
          <div className="inline-block bg-white/10 backdrop-blur-sm px-6 py-3 rounded-full border border-white/20">
            <p>مرجع أساسي لمعلمي الكيمياء ومحضري المختبرات</p>
          </div>
        </div>
      </div>

      {/* Intro Section */}
      <div className="max-w-4xl mx-auto px-4 py-12 text-center">
        <p className="text-lg text-slate-600 leading-relaxed">
          تم إعداد هذا الدليل ليكون مرجعاً أساسياً في مدارس المملكة. يهدف إلى تعزيز بيئة عمل آمنة وفعالة، ويقدم خلاصة مركزة لأهم الإجراءات والمعايير المعتمدة لضمان سلامة الطلاب وتميز الأداء المخبري.
        </p>
      </div>

      {/* Duties Section */}
      <section id="duties" className="py-16 bg-white">
        <div className="max-w-6xl mx-auto px-4">
          <div className="flex items-center gap-3 mb-10 border-r-4 border-cyan-500 pr-4">
            <ClipboardList className="text-cyan-600" size={32} />
            <h2 className="text-3xl font-bold text-slate-800">أولاً: المحضر وواجباته</h2>
          </div>

          <div className="grid md:grid-cols-2 gap-8">
            {/* Admin Duties */}
            <div className="bg-slate-50 p-6 rounded-xl border border-slate-200 shadow-sm hover:shadow-md transition-shadow">
              <h3 className="text-xl font-bold text-blue-800 mb-4 flex items-center gap-2">
                <span className="bg-blue-100 p-2 rounded-lg"><ClipboardList size={20} /></span>
                المهام الإدارية
              </h3>
              <ul className="space-y-3">
                {[
                  'استلام عهدة المختبر وتسجيلها وفق الإجراءات الرسمية.',
                  'التنسيق مع الإدارة لتوفير المواد والأجهزة الناقصة.',
                  'تنظيم السجلات لتوثيق التجارب واستهلاك المواد.',
                  'إعداد الملفات (النواقص، الاستهلاك، التعاميم).',
                  'المشاركة في جرد العهدة بنهاية العام.'
                ].map((item, idx) => (
                  <li key={idx} className="flex items-start gap-2 text-slate-700">
                    <CheckCircle className="text-green-500 mt-1 flex-shrink-0" size={16} />
                    <span>{item}</span>
                  </li>
                ))}
              </ul>
            </div>

            {/* Technical Duties */}
            <div className="bg-slate-50 p-6 rounded-xl border border-slate-200 shadow-sm hover:shadow-md transition-shadow">
              <h3 className="text-xl font-bold text-cyan-800 mb-4 flex items-center gap-2">
                <span className="bg-cyan-100 p-2 rounded-lg"><FlaskConical size={20} /></span>
                المهام الفنية
              </h3>
              <ul className="space-y-3">
                {[
                  'تهيئة وتجهيز المختبر للتجارب العملية.',
                  'حفظ الأدوات والمواد وتصنيفها بدقة في الخزانات.',
                  'تنظيف آثار التجارب وإعادة ترتيب الأدوات.',
                  'التأكد من سلامة الأدوات والأجهزة عند الاستلام.'
                ].map((item, idx) => (
                  <li key={idx} className="flex items-start gap-2 text-slate-700">
                    <CheckCircle className="text-green-500 mt-1 flex-shrink-0" size={16} />
                    <span>{item}</span>
                  </li>
                ))}
              </ul>
            </div>
          </div>
        </div>
      </section>

      {/* Safety Section */}
      <section id="safety" className="py-16 bg-slate-100">
        <div className="max-w-6xl mx-auto px-4">
          <div className="flex items-center gap-3 mb-10 border-r-4 border-cyan-500 pr-4">
            <ShieldAlert className="text-cyan-600" size={32} />
            <h2 className="text-3xl font-bold text-slate-800">ثانياً: السلامة في المختبر</h2>
          </div>

          <div className="grid md:grid-cols-2 gap-8">
            <div className="bg-white p-6 rounded-xl shadow-sm">
              <h3 className="text-lg font-bold text-slate-800 mb-4 border-b pb-2">المواصفات الأساسية للمختبر الآمن</h3>
              <div className="space-y-4">
                <div className="flex items-center gap-3">
                  <div className="bg-orange-100 p-2 rounded text-orange-600"><DoorOpen /></div>
                  <div>
                    <p className="font-semibold">الموقع والمخارج</p>
                    <p className="text-sm text-slate-600">الدور الأرضي، مخرجان للطوارئ يفتحان للخارج.</p>
                  </div>
                </div>
                <div className="flex items-center gap-3">
                  <div className="bg-blue-100 p-2 rounded text-blue-600"><Wind /></div>
                  <div>
                    <p className="font-semibold">التهوية</p>
                    <p className="text-sm text-slate-600">نظام تهوية جيد وخزانة سحب غازات (Fume Hood).</p>
                  </div>
                </div>
                <div className="flex items-center gap-3">
                  <div className="bg-red-100 p-2 rounded text-red-600"><AlertTriangle /></div>
                  <div>
                    <p className="font-semibold">تجهيزات السلامة</p>
                    <p className="text-sm text-slate-600">نافورة غسيل عيون، دش، طفايات وبطانيات حريق.</p>
                  </div>
                </div>
              </div>
            </div>

            <div className="bg-white p-6 rounded-xl shadow-sm border-l-4 border-red-400">
              <h3 className="text-lg font-bold text-slate-800 mb-4 border-b pb-2">قواعد سلوك صارمة</h3>
              <ul className="space-y-2">
                <li className="flex items-center gap-2 text-red-700 font-medium">
                  <Ban size={18} /> يمنع الأكل والشرب والمزاح والجري.
                </li>
                <li className="flex items-center gap-2 text-red-700 font-medium">
                  <Ban size={18} /> يمنع سحب المواد الكيميائية بالفم.
                </li>
                <li className="flex items-center gap-2 text-red-700 font-medium">
                  <Ban size={18} /> يمنع العمل منفرداً دون إشراف.
                </li>
                <li className="flex items-center gap-2 text-green-700 font-medium mt-4">
                  <CheckCircle size={18} /> الالتزام بأدوات الحماية (نظارة، قفاز، معطف).
                </li>
                <li className="flex items-center gap-2 text-green-700 font-medium">
                  <CheckCircle size={18} /> الإبلاغ الفوري عن أي حادث.
                </li>
              </ul>
            </div>
          </div>
        </div>
      </section>

      {/* First Aid Section */}
      <section id="firstaid" className="py-16 bg-white">
        <div className="max-w-6xl mx-auto px-4">
          <div className="flex items-center gap-3 mb-10 border-r-4 border-red-500 pr-4">
            <Stethoscope className="text-red-600" size={32} />
            <h2 className="text-3xl font-bold text-slate-800">ثالثاً: الإسعافات الأولية</h2>
          </div>

          <div className="grid md:grid-cols-2 lg:grid-cols-4 gap-4">
            {/* Cut/Bleeding */}
            <div className="bg-red-50 p-4 rounded-lg border border-red-100">
              <Activity className="text-red-500 mb-2" size={32} />
              <h4 className="font-bold text-lg mb-2">الجروح والنزيف</h4>
              <p className="text-sm text-slate-700">اضغط بقطعة قماش نظيفة، ارفع العضو المصاب. لا تنزع القماش إذا تشبع بالدم بل أضف فوقه. انقل للمستشفى.</p>
            </div>
            {/* Acid Burns */}
            <div className="bg-yellow-50 p-4 rounded-lg border border-yellow-100">
              <AlertTriangle className="text-yellow-600 mb-2" size={32} />
              <h4 className="font-bold text-lg mb-2">حروق الأحماض</h4>
              <p className="text-sm text-slate-700">اغسل فوراً بالماء البارد، ثم بمحلول <strong>بيكربونات الصوديوم 8%</strong>.</p>
            </div>
            {/* Alkali Burns */}
            <div className="bg-orange-50 p-4 rounded-lg border border-orange-100">
              <FlaskConical className="text-orange-500 mb-2" size={32} />
              <h4 className="font-bold text-lg mb-2">حروق القلويات</h4>
              <p className="text-sm text-slate-700">اغسل فوراً بالماء البارد، ثم بمحلول <strong>حمض الأسيتيك 1%</strong>.</p>
            </div>
            {/* Eyes */}
            <div className="bg-blue-50 p-4 rounded-lg border border-blue-100">
              <Eye className="text-blue-500 mb-2" size={32} />
              <h4 className="font-bold text-lg mb-2">إصابات العين</h4>
              <p className="text-sm text-slate-700">
                حمض: اغسل بالماء ثم بيكربونات صوديوم 1%.<br/>
                قلوي: اغسل بالماء ثم حمض البوريك 1%.
              </p>
            </div>
          </div>
        </div>
      </section>

      {/* Chemicals Section */}
      <section id="chemicals" className="py-16 bg-gradient-to-b from-slate-100 to-white">
        <div className="max-w-6xl mx-auto px-4">
          <div className="flex items-center gap-3 mb-10 border-r-4 border-cyan-500 pr-4">
            <FlaskConical className="text-cyan-600" size={32} />
            <h2 className="text-3xl font-bold text-slate-800">رابعاً: إدارة المواد الكيميائية</h2>
          </div>

          <div className="grid lg:grid-cols-2 gap-8">
            <div>
              <h3 className="text-xl font-bold mb-4 flex items-center gap-2">
                <Box className="text-cyan-600" /> شروط التخزين الآمن
              </h3>
              <ul className="space-y-3 bg-white p-6 rounded-xl shadow-sm">
                <li className="flex gap-2"><CheckCircle className="text-cyan-500 w-5" /> الأرفف في مستوى النظر كحد أقصى.</li>
                <li className="flex gap-2"><CheckCircle className="text-cyan-500 w-5" /> العبوات الكبيرة والثقيلة في الأسفل.</li>
                <li className="flex gap-2"><CheckCircle className="text-cyan-500 w-5" /> الأحماض المركزة في صناديق خشبية بها رمل.</li>
                <li className="flex gap-2"><CheckCircle className="text-cyan-500 w-5" /> المواد السامة والمتفجرة في خزانات معزولة ومؤمنة.</li>
              </ul>
            </div>

            <div>
              <h3 className="text-xl font-bold mb-4 flex items-center gap-2 text-red-600">
                <Ban className="text-red-600" /> مواد لا تخلط (متنافرة)
              </h3>
              <div className="overflow-hidden rounded-xl border border-slate-200 shadow-sm">
                <table className="w-full text-sm text-right bg-white">
                  <thead className="bg-slate-100 text-slate-700">
                    <tr>
                      <th className="p-3">المادة</th>
                      <th className="p-3">يجب فصله عن</th>
                    </tr>
                  </thead>
                  <tbody className="divide-y divide-slate-100">
                    <tr>
                      <td className="p-3 font-semibold text-slate-700">حمض الخل</td>
                      <td className="p-3 text-slate-600">حمض النيتريك، البيرمنجنات</td>
                    </tr>
                    <tr>
                      <td className="p-3 font-semibold text-slate-700">الأسيتيلين</td>
                      <td className="p-3 text-slate-600">النحاس، الفضة، الزئبق، الكلور</td>
                    </tr>
                    <tr>
                      <td className="p-3 font-semibold text-slate-700">الأمونيا اللامائي</td>
                      <td className="p-3 text-slate-600">الزئبق، الكلور، البروم، اليود</td>
                    </tr>
                    <tr>
                      <td className="p-3 font-semibold text-slate-700">الصوديوم</td>
                      <td className="p-3 text-slate-600">الماء، رابع كلوريد الكربون</td>
                    </tr>
                  </tbody>
                </table>
              </div>
            </div>
          </div>
        </div>
      </section>

      {/* Solutions Section */}
      <section id="solutions" className="py-16 bg-cyan-900 text-white">
        <div className="max-w-6xl mx-auto px-4">
          <div className="flex flex-col md:flex-row items-center justify-between gap-8">
            <div className="flex-1">
              <div className="flex items-center gap-3 mb-6">
                <Droplets className="text-cyan-300" size={32} />
                <h2 className="text-3xl font-bold">خامساً: تحضير المحاليل</h2>
              </div>
              <div className="bg-white/10 p-6 rounded-xl backdrop-blur-sm border border-white/20 mb-6">
                <h3 className="text-2xl font-bold text-yellow-400 mb-2">القاعدة الذهبية</h3>
                <p className="text-lg">أضف <span className="font-bold underline">الحمض إلى الماء</span> دائماً، وليس العكس، لتجنب تناثر الحمض بسبب الحرارة الناتجة.</p>
              </div>
              <ul className="space-y-3 text-cyan-50">
                <li className="flex gap-2"><CheckCircle size={20} /> استخدم أواني زجاجية سميكة أو حوض فخاري عند تحضير المحاليل الطاردة للحرارة.</li>
                <li className="flex gap-2"><CheckCircle size={20} /> استخدم سدادات مطاطية للمحاليل القلوية (لتجنب التصاق الزجاج).</li>
                <li className="flex gap-2"><CheckCircle size={20} /> احفظ المحاليل الحساسة للضوء (نترات الفضة) في زجاجات بنية.</li>
              </ul>
            </div>
            <div className="w-full md:w-1/3 flex justify-center">
                <FlaskConical size={200} className="text-cyan-500 opacity-50" />
            </div>
          </div>
        </div>
      </section>

      {/* Fire Section */}
      <section id="fire" className="py-16 bg-white">
        <div className="max-w-6xl mx-auto px-4">
          <div className="flex items-center gap-3 mb-10 border-r-4 border-red-500 pr-4">
            <Flame className="text-red-600" size={32} />
            <h2 className="text-3xl font-bold text-slate-800">سابعاً: مكافحة الحرائق (P.A.S.S)</h2>
          </div>

          <div className="grid grid-cols-2 md:grid-cols-4 gap-4 text-center">
            <div className="p-4 bg-slate-50 rounded-lg">
              <div className="w-12 h-12 bg-red-100 text-red-600 rounded-full flex items-center justify-center mx-auto mb-3 font-bold text-xl">1</div>
              <h4 className="font-bold mb-2">اسحب</h4>
              <p className="text-sm text-slate-600">اسحب صمام الأمان بقوة.</p>
            </div>
            <div className="p-4 bg-slate-50 rounded-lg">
              <div className="w-12 h-12 bg-red-100 text-red-600 rounded-full flex items-center justify-center mx-auto mb-3 font-bold text-xl">2</div>
              <h4 className="font-bold mb-2">وجه</h4>
              <p className="text-sm text-slate-600">وجه الخرطوم إلى قاعدة اللهب.</p>
            </div>
            <div className="p-4 bg-slate-50 rounded-lg">
              <div className="w-12 h-12 bg-red-100 text-red-600 rounded-full flex items-center justify-center mx-auto mb-3 font-bold text-xl">3</div>
              <h4 className="font-bold mb-2">اضغط</h4>
              <p className="text-sm text-slate-600">اضغط على ذراع التشغيل.</p>
            </div>
            <div className="p-4 bg-slate-50 rounded-lg">
              <div className="w-12 h-12 bg-red-100 text-red-600 rounded-full flex items-center justify-center mx-auto mb-3 font-bold text-xl">4</div>
              <h4 className="font-bold mb-2">حرك</h4>
              <p className="text-sm text-slate-600">حرك الخرطوم يميناً ويساراً.</p>
            </div>
          </div>

          <div className="mt-8 bg-yellow-50 border-r-4 border-yellow-400 p-4 rounded text-slate-700">
            <strong className="block mb-1 text-yellow-800">احتياطات هامة:</strong>
            <ul className="list-disc list-inside text-sm">
              <li>حافظ على مسافة آمنة (3 إلى 6 أمتار).</li>
              <li>اجعل مخرج الطوارئ خلفك دائماً.</li>
              <li>إذا فقدت السيطرة، غادر فوراً وأغلق الباب خلفك.</li>
            </ul>
          </div>
        </div>
      </section>

      {/* Footer */}
      <footer className="bg-slate-800 text-slate-300 py-8 text-center">
        <div className="max-w-6xl mx-auto px-4">
          <p className="mb-2">السلامة أولاً: مسؤوليتنا المشتركة والتزامنا الدائم</p>
          <p className="text-sm text-slate-500">تم تحويل هذا الدليل إلى نسخة رقمية بناءً على تصميم "أبو عبد الرحمن"</p>
        </div>
      </footer>
    </div>
  );
};

export default SchoolLabGuide;

/
